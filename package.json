{
  "name": "seglab",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "verify": "bun verify.mjs",
    "verify:fast": "bun verify.mjs --fast",
    "verify:queue": "bun verify.mjs --wait=20m",
    "verify:plan": "bun verify.mjs --list",
    "models": "node scripts/download-models.mjs",
    "build:wasm": "./scripts/build-cv-wasm.sh"
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
