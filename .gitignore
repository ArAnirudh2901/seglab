# Headless-verify artifacts: Playwright profile + browser-cached models
# (hundreds of MB — regenerated by `bun verify.mjs`).
.cache/
.DS_Store

# Benchmark output (regenerated by `bun bench.mjs`).
bench-out/

# Model weights are exported locally (AGPL-3.0) — see models/README.md
models/*.onnx
models/vocabulary.json
